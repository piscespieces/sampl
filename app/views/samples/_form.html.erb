<%= form_with(model: sample) do |form| %>
  <% if sample.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sample.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul>
        <% sample.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :sample_pack_id, style: "display: block" %>
    <%= form.text_field :sample_pack_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :key, style: "display: block" %>
    <%= form.text_field :key %>
  </div>

  <div>
    <%= form.label :bpm, style: "display: block" %>
    <%= form.number_field :bpm %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
